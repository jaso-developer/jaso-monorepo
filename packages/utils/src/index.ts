/**
 * @jaso/utils — Punto de entrada principal.
 *
 * Puedes importar todo:
 *   import { slugify, formatDate } from "@jaso/utils"
 *
 * O solo lo que necesitas (mejor para tree-shaking):
 *   import { slugify } from "@jaso/utils/strings"
 *   import { formatDate } from "@jaso/utils/dates"
 */
export * from "./dates.ts"
export * from "./strings.ts"
export * from "./seo.ts"
